import * as net from 'net';
declare const SocketsPool: (sockets?: Set<net.Socket>) => {
    onConnection: (socket: net.Socket) => void;
    closeAll: () => Promise<void>;
};
export default SocketsPool;
