<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <div class="center" style="display:flex; justify-content: center;  margin: 0;">
        Thank you <%= accessToken %>
    </div>
</body>
<script>
    document.addEventListener("DOMContentLoaded", async () => {

        const url = new URL(window.location.href);

        // Get the value of the 'accessToken' parameter from the URL
        // const accesstoken = url.searchParams.get("accesstoken");
        // const accessToken = '<%= accessToken %>';

        // Check if cookies are set
        if (document.cookie) {
            // var cookies = document.cookie.split(';');
            // console.log(typeof cookies[1])
            // var token = cookies[1].split("=");
            // console.log(token);
            // console.log('sending msg to Hello Channel')

            try {
                Hello.postMessage(`<%=accessToken%>`);
            } catch (error) {
                window.location.href = "https://freecash.enactweb.com/"
            }
        } else {
            console.log('No cookies set');
        }
    })
</script>

</html>